{
  "version": 2,
  "builds": [{ "src": "server.js", "use": "@now/node-server" }],
  "routes": [{ "src": "/.*", "dest": "server.js" }],
  "env": {
    "MONGO_URI": "@my-mongo-uri",
    "SECRET": "@my-secret",
    "PORT": "@my-port"
  }
}
